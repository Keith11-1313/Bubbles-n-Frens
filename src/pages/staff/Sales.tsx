// Staff Sales page — identical to Manager Sales (same cart + product grid)
// Reuses the same component since sales functionality is the same for both roles
export { default } from '../manager/Sales'
